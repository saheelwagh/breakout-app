<nav class="bg-indigo-600 shadow-md"> {/* Changed to indigo for a nice purple shade */}
    <div class="container mx-auto px-4 py-3">
      <div class="flex justify-between items-center">
  
        {/* Logo */}
        <a href="/" class="text-2xl font-bold text-white">
          Logo
        </a>
  
        {/* Search Bar - Hidden on mobile (screens smaller than md), centered on medium screens and up */}
        <div class="hidden md:flex flex-grow justify-center px-4">
          <input
            type="search"
            placeholder="Search for ads..."
            class="w-full max-w-lg px-4 py-2 rounded-md border border-indigo-400 bg-indigo-500 text-white placeholder-indigo-200 focus:outline-none focus:ring-2 focus:ring-white focus:bg-indigo-400 transition duration-200"
          />
        </div>
  
        {/* Login Button */}
        <button class="bg-white text-indigo-600 font-semibold px-4 py-2 rounded-md hover:bg-indigo-100 transition duration-200">
          Login
        </button>
  
      </div>
    </div>
  </nav>

<!-- 
**Explanation:**

1.  **Background & Text:** `bg-indigo-600` provides a nice purple background. `text-white` is used for the logo for contrast.
2.  **Layout:** `flex justify-between items-center` arranges the main items (logo, search container, button) horizontally, spaces them out, and aligns them vertically.
3.  **Search Bar Responsiveness:**
    * `hidden md:flex`: The `div` containing the search bar is `hidden` by default (on mobile). It becomes a `flex` container (making it visible) starting from the `md` breakpoint (medium screens, typically 768px) upwards.
    * `flex-grow justify-center`: On larger screens, this allows the search bar container to take up available space and centers the input within it.
4.  **Search Bar Styling:** Added some padding, rounded corners, and distinct background/text colors for visibility within the purple bar, plus focus styles.
5.  **Login Button Styling:** Uses a contrasting background (`bg-white`) and text color (`text-indigo-600`) with a hover effect.

--- -->